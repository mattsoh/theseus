<% content_for :head do %>
  <%= vite_javascript_tag "taggable" %>
<% end %>

<% selected_tags = taggable.try(:tags) || [] %>
<% all_tags = available_tags(selected_tags) %>

<div style="margin-bottom: 16px;">
  <%= form.label :tags, "Tags", style: "display: block; font-size: 14px; font-weight: 600; margin-bottom: 4px;" %>
  <%= form.select :tags,
                  all_tags,
                  { selected: selected_tags, include_blank: false },
                  { multiple: true,
                    class: "selectize-tags",
                    style: "width: 100%;",
                    data: {
                      selectize: {
                        plugins: ['remove_button'],
                        persist: false,
                        create: true,
                        maxItems: nil
                      }
                    }
                  } %>
  <p style="color: var(--fgColor-muted); font-size: 12px; margin-top: 4px;">Select from common tags or create your own</p>
</div>
