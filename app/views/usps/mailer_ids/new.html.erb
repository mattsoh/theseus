<% content_for :title, "New Mailer ID" %>

<div class="page-container--xs">
  <h1 class="page-title" style="margin-bottom: 24px;">New Mailer ID</h1>
  <%= render "form", usps_mailer_id: @usps_mailer_id %>
  <div style="margin-top: 32px;">
    <%= render Primer::Beta::Button.new(tag: :a, href: usps_mailer_ids_path, scheme: :invisible, size: :small) do |btn|
      btn.with_leading_visual_icon(icon: :"arrow-left")
      "Back to mailer IDs"
    end %>
  </div>
</div>
