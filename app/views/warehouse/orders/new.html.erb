<% content_for :title, "New order" %>

<div style="max-width: 800px; margin: 0 auto; padding: 24px;">
  <h1 style="font-size: 24px; font-weight: 600; margin: 0 0 24px 0;">New order</h1>

  <%= render "form", warehouse_order: @warehouse_order %>

  <div style="margin-top: 32px;">
    <%= link_to "← Back to orders", warehouse_orders_path, style: "color: var(--fgColor-muted); text-decoration: none;" %>
  </div>
</div>
