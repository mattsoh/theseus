<% scheme = case order.aasm.current_state
  when :draft then :secondary
  when :dispatched then :accent
  when :mailed then :success
  when :errored then :danger
  when :canceled then :attention
  else :secondary
  end %>
<%= render(Primer::Beta::Label.new(scheme: scheme)) { order.humanized_state } %>
