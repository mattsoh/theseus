<div style="display: flex; align-items: center; gap: 8px;">
  <%= link_to line_item.sku.name, line_item.sku, style: "color: var(--fgColor-accent); font-weight: 500;" %>
  <%= render partial: "admin_inspector", locals: {record: line_item} if params[:inspect_line_items] %>
</div>
